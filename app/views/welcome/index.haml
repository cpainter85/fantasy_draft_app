.container

  =link_to 'Create New Game', new_game_path, class: 'btn btn-primary'

  -@games.each do |game|
    %p
      =link_to game.name, game_path(game)
      ="(#{pluralize(game.teams.count, 'team')}) - #{game.rounds} Rounds"
      -if !(current_user && current_user.participant?(game))
        =link_to 'Join this game', new_game_team_path(game)
